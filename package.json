{
  "name": "charons-gamble-mvp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npx --yes vite",
    "build": "npx --yes tsc -b && npx --yes vite build",
    "preview": "npx --yes vite preview",
    "check": "npx --yes tsc --noEmit"
  },
  "dependencies": {
    "vite": "^5.4.11"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
